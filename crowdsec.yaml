services:
  crowdsec:
    image: crowdsecurity/crowdsec
    restart: always
    environment:
      COLLECTIONS: "crowdsecurity/nginx"
      GID: "${GID-1000}"
    depends_on:
      - 'reverse-proxy'
    volumes:
      - ./crowdsec/acquis.yaml:/etc/crowdsec/acquis.yaml
#     - logs:/var/log/nginx
      - /root/crowdsec/data:/var/lib/crowdsec/data/
      - /root/crowdsec/config:/etc/crowdsec/